{
  "name": "my-monorepo",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "verify": "pnpm install --frozen-lockfile && turbo build lint type-check",
    "compose:local": "docker compose -f docker-compose.yml -f docker-compose.local.yml",
    "db:up": "pnpm compose:local --profile dev up -d",
    "db:down": "pnpm compose:local --profile dev down",
    "db:reset": "pnpm compose:local --profile dev down -v && pnpm compose:local --profile dev up -d",
    "db:setup": "pnpm compose:local --profile dev up -d --wait postgres && pnpm --filter api db:migrate",
    "docker:build": "pnpm compose:local --profile full build",
    "docker:up": "pnpm compose:local --profile full up",
    "docker:up:detached": "pnpm compose:local --profile full up -d",
    "docker:down": "pnpm compose:local --profile full down",
    "docker:logs": "pnpm compose:local --profile full logs -f",
    "docker:prod": "docker compose -f docker-compose.yml -f docker-compose.prod.yml --profile full up -d --build",
    "docker:prod:down": "docker compose -f docker-compose.yml -f docker-compose.prod.yml --profile full down"
  },
  "devDependencies": {
    "turbo": "2.9.9",
    "typescript": "5.9.3"
  }
}
