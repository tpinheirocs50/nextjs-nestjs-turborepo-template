# Node environment
NODE_ENV=development

# Port the API listens on
PORT=3001

# CORS allowed origin (the web app's URL)
CORS_ORIGIN=http://localhost:3000

DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:5432/monorepo?schema=public"